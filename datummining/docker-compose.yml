version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: datummining_datum_1
      APP_PORT: 20000

  datum:
    image: ghcr.io/bugoao/datum_gateway:1368
    entrypoint: ["/app/datum_gateway"]
    command: ["--config=/app/conf/datum_gateway_config.json"]
    user: 1000:1000
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data/settings/:/app/conf/
    ports:
      # datum gateway port
      - 23333:23334
